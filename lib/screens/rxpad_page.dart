import 'package:flutter/material.dart';

class RxpadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Rxpad Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
